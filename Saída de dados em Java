importar java. util. Localidade;

Programa de aula pública {
	public static void main(String[] args) {
		imprimir mensagem 
		Sistema. fora. println("Olá Mundo");
		Sistema. fora. println("Bom dia");

		Para escrever o conteúdo de uma variável de algum tipo básico
		int y = 32;
		Sistema. fora. println(Y);

		Para escrever o conteúdo de uma variável com ponto flutuante 
		duplo x = 10,35784;
		Sistema. fora. println(x);
		Sistema. fora. printf("%.2f%n", x);
		Sistema. fora. printf("%.4f%n", x);
		Localidade. setDefault(Localidade. NOS);
		Sistema. fora. printf("%.4f%n", x);

		Para concatenar vários elementos em um mesmo comando de escrita

		Sistema. fora. println("Resultado = " + x + " METROS");

		Para concatenar vários elementos em um mesmo comando de escrita
		Sistema. fora. printf("RESULTADO = %.2f metros%n", x);

		Para concatenar vários elementos em um mesmo comando de escrita com muitos elementos
		Nome da string = "Maria";
		int idade = 31;
		renda dupla  = 4000,0;
		Sistema. fora. printf("%s tem %d anos e ganha R$ %.2f reais%n", nome, idade, renda);
	}
}
